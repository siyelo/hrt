%table.data
  %thead
    %tr.head
      %th Name
      %th.align= "Expenditure (#{@report.unit})"
      %th.align= "Budget (#{@report.unit})"

  %tbody
    - @report.collection.each do |e|
      %tr
        %td= resource_link(@report, e)
        %td.align= @report.format(e.total_spend)
        %td.align= @report.format(e.total_budget)
