%h2
  Recent Comments

%ul.dashboard_comments
  - if comments.present?
    - comments.each do |comment|
      - if comment.commentable
        %li
          .avatar
            = image_tag comment.user.gravatar(22)
          .comment
            .author
              .left
                = mail_to comment.user.email, comment.user.name
                == #{time_ago_in_words(comment.created_at)} ago
              .right
                - comment_title = truncate(model_name(comment.commentable), :length => 40)
                %span= link_to comment_title, commentable_path(comment, current_user)
            %p= truncate(comment.comment, :length => 300)
  - else
    %li.nothing There are no comments posted in the last 6 months.

  %li.view-all= link_to "view all", current_user.sysadmin? ? admin_comments_path : comments_path