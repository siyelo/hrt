.section_overlap
  %ul.section_nav
    - ["Reporting", "Non-Reporting", "All"].each do |filter|
      - css_class = "selected" if params[:filter] == filter || (filter == 'Reporting' && params[:filter].blank?)
      %li{:class => css_class}
        = link_to filter, admin_organizations_path({:sort => params[:sort], :direction => params[:direction], :query => params[:query], :filter => filter})
