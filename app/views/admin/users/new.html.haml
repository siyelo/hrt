- title("New User")

%h1.main_heading Create New User

= render 'shared/error_messages', :resource => @user

= form_for @user, :builder => CustomFormBuilder, :url => admin_users_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
