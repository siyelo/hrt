- title("New User")

%h1.main_heading Create New User

= render 'shared/error_messages', :resource => @user

= semantic_form_for [:admin, @user], :url => admin_users_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
