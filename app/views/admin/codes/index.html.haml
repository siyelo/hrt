= render :partial => 'admin/shared/codes_menu'

.section_form.single.connect.topless
  #search.push
    = form_tag admin_codes_path, method: :get do
      = render 'shared/search_form'

  %ul.activity_tree
    - @codes.each do |code|
      = render 'code_row', code: code

