- title("New Code")

%h1.main_heading Create New Code

= render 'shared/error_messages', :resource => @code

= semantic_form_for [:admin, @code], :url => admin_codes_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
