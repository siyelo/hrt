- title("Edit Code")

%h1.main_heading Edit Code

= render 'shared/error_messages', :resource => @code

= semantic_form_for @code, :as => :code, :url => admin_code_path(@code), :html => {:method => :put, :class => "basic_form js_form"} do |f|
  = render 'form', :f => f
