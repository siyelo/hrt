%h1.main_heading Edit Currency

= error_messages_for :organization, :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for [:admin, @currency], :html => {:class => 'basic_form js_form'} do |f|
  = render 'form', :f => f
