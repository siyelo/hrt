%h1.main_heading New Currency

= render 'shared/error_messages', :resource => @currency

%h2 New currency information
.section_base
  = form_for [:admin, @currency], builder: CustomFormBuilder, :html => {:class => 'basic_form js_form'} do |f|
    = render 'form', :f => f
