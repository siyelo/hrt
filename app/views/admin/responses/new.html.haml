%h1.main_heading
  New Response

= error_messages_for :response,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @response, :url => admin_responses_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
