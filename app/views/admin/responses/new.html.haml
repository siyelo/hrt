%h1.main_heading
  New Response

= render 'shared/error_messages', :resource => @response

= semantic_form_for @response, :url => admin_responses_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
