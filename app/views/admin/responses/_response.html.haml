- organization = response.organization

%tr
  %td.title= link_to response_title(response), projects_path(:response_id => response.id) if response
  %td= link_to organization.name, edit_admin_organization_path(organization)
  %td= response.status
