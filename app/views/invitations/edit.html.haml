-title "New Profile Registration"

.sign_in_form.center
  %h2.welcome
    Welcome to the
    %br Health Resource Tracker
  %h3 Please select a password

  = semantic_form_for @user, :url => invitation_url(params[:id]) do |f|
    = f.inputs do
      %ul
        = f.input :password, :value => nil
        = f.input :password_confirmation

    = f.actions do
      = f.action :submit, :label => "Save", :class => "last", :button_html => {:class => "create_alt"}
