%h1.main_heading
  New Activity
  %span= render 'activities/help'

= render 'shared/error_messages', :resource => @activity

= semantic_form_for @activity, :url => activities_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'shared/outlays/form', :f => f, :outlay => @activity
