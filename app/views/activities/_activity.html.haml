%tr.js_project_row
  %td{:colspan => 2}
    = link_to activity.name.presence || 'Unnamed Activity', edit_activity_path(activity), :title => activity.description, :class => 'tooltip-S'

  %td
    .right_justified= n2c activity.total_spend
  %td
    .right_justified= n2c activity.total_budget
