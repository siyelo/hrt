#delayed_import.simple_overlay
  %h3.title_bar Upload a large file for processing
  .section_base
    %p As the System Administrator, you can upload large files here.  Beware! Any projects, activities or implementer splits uploaded here will bypass the normal validation checks - please ensure that there are no problems with the data in the file before uploading it.
    %br
    - form_tag(import_and_save_projects_path(url_params), :multipart => true) do
      .upbox
        = label_tag :file, "File"
        = file_field_tag :file
      = submit_tag "Import", :class => 'small_submit_button', :confirm => 'This is a potentially harmful action, are you sure you want to continue?'
