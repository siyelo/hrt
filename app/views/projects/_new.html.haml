%h1.main_heading
  New Project
  %span= render 'help'

= error_messages_for :project,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @project, :url => projects_path, :html => {:class => "basic_form js_form"} do |f|
  = render 'form', :f => f
