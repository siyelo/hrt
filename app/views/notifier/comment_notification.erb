Hello,
<%= @comment.user.name %> <<%= @comment.user.email %>> has made a new comment on the <%= @comment.commentable.name %> <%= @comment.commentable.class.to_s.titleize %>:


<%= @comment.comment %>


To view/respond to it, please visit the url below.

<% if @comment.commentable.is_a?(DataResponse) %>
  <%= projects_url %>
<% else %>
  <%= activity_url(@comment.commentable) %>
<% end %>


<%= render 'signature' %>
