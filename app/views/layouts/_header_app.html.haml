#header_app
  .wrapper
    #logo
      - org_name = current_user.organization.name
      - org_name = @response.organization.name if @response
      %p= truncate(org_name, :length => 35)

    .right
      %a{:href => '/'}
        = image_tag "rwanda-coat-of-arms-med.png"

    = render :partial => '/layouts/login_nav'
